//Convert all the strings to title caps in a string array
var animals=["cat","rat","cow","monkey"];
var arr=a.map(item)=>{return
item[0].toUpperCase()+item.substr(1).toLowercase()};
console.log(arr);
