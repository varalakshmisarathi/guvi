#circumference of a circle
def circumference(r):
#calculate circumference
    c=2*22/7*r
    return c
#get input
r=float(input())
#check radius
if(r<0):
        print("error")
else:
        v=circumference(r)
        l=round(v,2)
        print(l,end="")
