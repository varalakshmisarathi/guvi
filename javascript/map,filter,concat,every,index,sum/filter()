const readline=require('readline');
const inp=readline.createInterface({
  input:process.stdin
});
const a=[];
inp.on('line',(data)=>{
  a.push(data);
});
inp.on('close',()=>{
   var b=a[0].split(" ");
  var c=[];
    function filter(){
      for(var i=0;i<b.length;i++)
      {
         if(b[i]>50)
         {
             c.push(b[i]);
         }
      }
      return c.join(" ");
    }
    var res=filter();
    console.log(res);
});
//input
30 50 55
output
55
