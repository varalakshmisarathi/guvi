//7 - findLastIndex
//This method is like Array.prototype.findIndex except that it iterates over elements of collection from right to left.
function index(array)
{
var result = array.lastIndexOf(8);
console.log(result);
}
index([5, 12, 6, 130, 8]);
